{
  "name": "@presence/server",
  "version": "0.1.0",
  "private": true,
  "description": "Dumb WebSocket relay for file-level presence.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "tsc -p . && node dist/index.js",
    "dev": "tsc -p . --watch"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.0"
  }
}
